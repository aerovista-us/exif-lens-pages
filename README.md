# EXIF Lens

Static Next.js frontend for the EXIF Lens metadata inspector.

The API base is compiled from `NEXT_PUBLIC_API_BASE`. For GitHub Pages, set the repository variable `NEXT_PUBLIC_API_BASE` to the HTTPS URL of the deployed EXIF Lens API.
